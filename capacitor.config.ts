import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.hinonso716.financialtracker',
  appName: 'Financial Tracker',
  webDir: 'dist-native',
}

export default config
